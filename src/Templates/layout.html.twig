<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css"/>
    <!--<link href="/css/bootstrap-datetimepicker.min.css" rel="stylesheet">-->
    <!--<link href="/css/daterangepicker.css" rel="stylesheet">-->
    <link rel="stylesheet" type="text/css" href="/css/custom.css">
    <link rel="icon" href="/favicon.ico" type="image/x-icon"/>
    <link rel="icon" type="image/png" href="data:;base64,iVBORw0KGgo=">
</head>
<body>
    <div id="loader-wrapper">
        <div id="loader"></div>
    </div>
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-3 col-md-2 sidebar">
                <ul class="nav nav-sidebar">
                    {% for item in menu%}
                    <li {% if item.active %} class="active" {% endif %}>
                        <a href="{{ item.url}}"><span class="glyphicon glyphicon-{{ item.icon }}">&nbsp;</span>{{ item.label }}</a>
                    </li>
                    {% endfor%}
                </ul>
            </div>

            <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
                <div class="row">
                    <div class="col-sm-12 col-md-12">
                    {% for alertType, alerts in app.session.flashbag.all %}
                        {% for alert in alerts %}
                            <div class="alert alert-{{ alertType }}">
                                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                                <strong>{{ alert }}</strong>
                            </div>
                        {% endfor %}
                    {% endfor %}
                    {% if env == 'development' %}
                    <div id="demo-data" class="text-center">
                        <span class="alert alert-warning">Test Environment</span>
                    </div>
                    {% endif%}
                    {% block content %}{% endblock %}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="/js/jquery-3.3.1.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    {% block javascript %}{% endblock %}
</body>
</html>
